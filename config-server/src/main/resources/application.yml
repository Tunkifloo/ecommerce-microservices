server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: "file:///C:/Users/nicol/OneDrive/Documentos/Software Projects/Me/Arq-Software/ecommerce-config-repo"
          default-label: main
          clone-on-start: true
          force-pull: true

# Actuator para monitoreo
management:
  endpoints:
    web:
      exposure:
        include: health,info,refresh,env
  endpoint:
    health:
      show-details: always

# Logging
logging:
  level:
    org.springframework.cloud.config: DEBUG
    org.springframework.boot: INFO